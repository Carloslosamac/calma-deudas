## Objetivo
Cambiar el texto del botón compacto del header en móvil de "Analizar" a "Consulta".

## Cambio en `src/components/Header.tsx`
- En el botón CTA compacto, sustituir el texto móvil `<span className="lg:hidden">Analizar</span>` por `Consulta`.
- El texto de escritorio ("Analizar mi deuda") y el CTA dentro del menú se mantienen igual.
